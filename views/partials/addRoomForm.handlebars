{{!-- 
<form action="/rooms/addRoomForm" method="POST">
    <fieldset>
        <legend>Room Details</legend>
        
        <label for="roomName">Room Name:</label>
        <input type="text" id="roomName" name="roomName" required><br>
        
        <label for="pricingPerNight">Pricing Per Night:</label>
        <input type="number" id="pricingPerNight" name="pricingPerNight" min="10" required><br>

        <label for="balcony">Balcony:</label>
        <select id="balcony" name="balcony" required>
            <option value="true">Yes</option>
            <option value="false">No</option>
        </select><br>
        
        <label for="bedSizes">Bed Sizes (comma-separated):</label>
        <input type="text" id="bedSizes" name="bedSizes" 
               placeholder="e.g., Queen, Twin" 
               pattern="^[a-zA-Z\s]+(,\s*[a-zA-Z\s]+)*$" 
               aria-describedby="bedSizesHint" 
               required>
        <small id="bedSizesHint">Separate bed sizes with commas (e.g., Queen, Twin).</small><br>
    </fieldset>
    
    <button type="submit">Add Room</button>
    <button type="reset">Reset</button>
</form> --}}
